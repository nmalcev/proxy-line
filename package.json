{
  "name": "proxy_monkey",
  "version": "0.2.0",
  "description": "Tool for proxying web requests",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "n.maltsev",
  "license": "ISC",
  "devDependencies": {
    "ssl-root-cas": "^1.3.1"
  }
}
